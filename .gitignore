node_modules/
dist/
*.log
.DS_Store

# Demo data is regenerated by `npm run seed`. The audit log is a runtime
# artifact and should not be committed.
data/audit.jsonl

# Účty (bcrypt hashe hesel) a klíč pro podpis session cookie. Runtime
# artefakty s tajemstvím — do gitu nepatří.
data/users.json
data/session.key

# Videa zapuštěných prvků (díry v zemi). Lokálně je DATA_ROOT uvnitř repa,
# na serveru je mimo něj; stovky MB mp4 do gitu nepatří.
data/Cervori/
data/Paleonaleziste/

# Lokální proměnné prostředí (tajemství). Šablona je .env.example (ta v gitu zůstává).
.env
# Archivy (zálohy, exporty, podklady) do gitu nepatří.
*.zip
handoff.md

# Data displeju patri jen na server, ne do gitu
data/
mapovani.json
